Stackato App for Stackato documentation
=======================================

1. Build the docs with 'make publicdocs'
2. Copy all files (but this one) from this directory into the base directory of the build (e.g. _build/public-docs/).
3. 'cd' to the build directory and 'stackato target' your Paas
4. 'stackato push -n [appname]'

